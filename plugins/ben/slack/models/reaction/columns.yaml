# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    message_id:
        label: Message
        relation: message
        select: id

    user_id:
        label: User
        relation: user
        select: username

    emoji:
        label: Emoji
        type: text

    created_at:
        label: Created
        type: datetime

    updated_at:
        label: Updated
        type: datetime
